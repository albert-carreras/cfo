# AGENTS.md

This project's agent guidance lives in **[CLAUDE.md](CLAUDE.md)** — read it first.

It covers the one rule (trust core before voice), the invariants you must not break, the stack and
conventions, the repo layout, and where to read the full spec (`docs/`).
